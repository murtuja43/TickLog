<?xml version="1.0" encoding="utf-8"?>
<!--
    The XML theme only governs the brief moment between process start and the
    first Compose frame (the window background). Everything visible afterwards is
    themed entirely in Compose via TickLogTheme. We extend a bare platform theme
    so no Material Components XML dependency is required.
-->
<resources>
    <style name="Theme.TickLog" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!--
        Launch (splash) theme: a minimal, centred TickLog mark on a white field,
        handed off to Theme.TickLog the moment the first Compose frame is ready.
        Backed by androidx.core:core-splashscreen so it behaves identically from
        API 28 up through the platform splash on API 31+.
    -->
    <style name="Theme.TickLog.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.TickLog</item>
    </style>
</resources>
