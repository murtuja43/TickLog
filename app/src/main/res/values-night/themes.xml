<?xml version="1.0" encoding="utf-8"?>
<!--
    Dark-mode counterpart of the window theme: a pure-black background so the
    launch window matches the dark Compose UI with no white flash.
-->
<resources>
    <style name="Theme.TickLog" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!--
        Dark-mode splash: the white mark on a pure-black field. The animated-icon
        drawable is auto-swapped for its drawable-night (white) variant.
    -->
    <style name="Theme.TickLog.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.TickLog</item>
    </style>
</resources>
